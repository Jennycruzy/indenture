{
  "name": "@indenture/offramp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "VEIL off-ramp listener — fires a SANDBOX fiat payout on a genuine on-chain corridor clear, gated by an officer-decrypt of `moved > 0`. Provider-agnostic seam; Flutterwave v4 adapter included.",
  "license": "BSD-3-Clause-Clear",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "viem": "^2.47.12"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
