{
  "name": "@cloistra/offramp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CLOISTRA off-ramp listener — fires a SANDBOX fiat payout on a genuine on-chain corridor clear, gated by an officer-decrypt of `moved > 0`. Provider-agnostic seam; Flutterwave v3 adapter included (Nigeria/NGN).",
  "license": "BSD-3-Clause-Clear",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@zama-fhe/sdk": "^3.0.0",
    "dotenv": "^16.4.5",
    "viem": "^2.47.12"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
